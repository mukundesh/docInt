image_root: ../.img        
pipeline:
  - name: gcv_recognizer
    config:
      bucket: pedia

  - name: html_generator
    config:
      image_root: ../.img      
      html_root: html
      color_dict:
        word: blue
